[build-system]
requires = ["cython>=3.0", "setuptools>=58.0.4", "wheel"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
skip = "cp36-* cp37-* cp38-* cp39-* pp37-* pp38-* pp39-*"
test-requires = "pytest"
test-command = "pytest {project}/tests"
